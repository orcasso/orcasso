doctrine_migrations:
    migrations_paths:
        'DoctrineMigrations': '%kernel.project_dir%/migrations'
    enable_profiler: '%kernel.debug%'

    storage:
        table_storage:
            table_name: 't_migration'

    organize_migrations: 'BY_YEAR'
