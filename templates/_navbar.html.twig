<nav class="main-header navbar navbar-expand navbar-white navbar-light">
    <!-- Left navbar links -->
    <ul class="navbar-nav">
        <li class="nav-item">
            <a class="nav-link" data-widget="pushmenu" href="#" role="button"><i class="fas fa-bars"></i></a>
        </li>
    </ul>
    <!-- /.navbar-nav -->

    <!-- Right navbar links -->
    {% if app.user %}
        <ul class="navbar-nav ml-auto">
            <!-- User Account Menu -->
            <li class="dropdown user user-menu">
                <!-- Menu Toggle Button -->
                <a class="dropdown-toggle" data-toggle="dropdown" href="#">
                    <i class="fa fa-user fa-fw"></i>
                    {{ app.user.email }}
                </a>
                {% block navbar_user %}
                    <ul class="dropdown-menu text-center" id="user-dropdown-menu">
                        <li class="dropdown-item">
                            <a href="{{ path('security_authentication_logout') }}" class="btn">
                                <i class="fa fa-power-off"></i> {{ 'meta.word.logout'|trans }}
                            </a>
                        </li>
                    </ul>
                {% endblock navbar_user %}
            </li>
            <!-- /.user-menu -->
        </ul>
    {% endif %}
</nav>
<!-- /.navbar -->
